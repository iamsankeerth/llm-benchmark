# Core dependencies
ollama>=0.4.0
pydantic>=2.0
pynvml>=12.0
psutil>=5.9
pandas>=2.0
matplotlib>=3.8
rich>=13.0
tabulate>=0.9
fastapi>=0.100.0
uvicorn>=0.23.0
huggingface_hub>=0.20.0
requests>=2.31.0

# HuggingFace Transformers runner (optional — for HF-only models)
# Uncomment these if you want to benchmark HF Transformers models:
# torch>=2.0
# transformers>=4.40.0
# accelerate>=0.27.0
# bitsandbytes>=0.42.0

# vLLM runner (optional — for AWQ/GPTQ/MoE models)
# Uncomment this if you want to benchmark quantized models via vLLM:
# vllm>=0.4.0
